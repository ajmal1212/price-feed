version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    environment:
      - BACKEND_URL=http://172.16.1.3
      - BACKEND_HOST=pulse.gopocket.in
    restart: always
